# ============================================================================
#  Сценарий сборки главного компонента (main) проекта jd9165_hello_display
# ----------------------------------------------------------------------------
#  Один исходный файл main.c; всё остальное (драйвер панели, esp_lcd,
#  драйверы LDO/LEDC) подтягивается из ESP-IDF и реестра компонентов.
# ============================================================================

# Регистрация компонента: исходник main.c, текущая папка — в путях поиска
idf_component_register(SRCS "main.c" INCLUDE_DIRS ".")
