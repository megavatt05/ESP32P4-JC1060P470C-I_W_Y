# ============================================================================
#  Корневой сценарий сборки проекта jd9165_hello_display
#  Плата: Guition JC1060P470C (ESP32-P4 rev v1.3) | ESP-IDF v5.3+
# ----------------------------------------------------------------------------
#  Порядок строк менять нельзя — сборочная система ESP-IDF требует
#  строгой последовательности объявлений.
# ============================================================================

# Минимальная версия CMake, требуемая ESP-IDF
cmake_minimum_required(VERSION 3.5)

# Ограничение длины путей объектных файлов — обход ошибок на Windows
set(CMAKE_OBJECT_PATH_MAX 512)

# Подключение сборочной инфраструктуры ESP-IDF
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Имя проекта = имя прошивки (jd9165_hello_display.bin)
project(jd9165_hello_display VERSION 1.0.0)
